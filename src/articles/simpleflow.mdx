export const metadata = {
  title: "Simple Flow Animation",
  slug: "simpleflow",
  description: "Learn how to create smooth flow animations using SVG paths and CSS transitions.",
  publishDate: "2025-10-01",
  tags: ["svg", "animation", "flow"]
};

import { Sandpack } from "@codesandbox/sandpack-react";

# Simple Flow Animation

This article explores the fundamentals of creating smooth flow animations with SVG paths. We'll start with basic concepts and build up to more complex examples.

---

## Getting Started

Flow animations are a great way to guide users' attention and create dynamic, engaging interfaces. The key is understanding how SVG paths work and how to animate them effectively.

<div className="code-block">
<Sandpack
    theme="dark"
    template="react"
    showOpenInCodeSandbox={false}
    options={{
        showNavigator: false,
        showLineNumbers: true,
        showInlineErrors: true,
        editorHeight: 400,
        editorWidthPercentage: 50,
    }}
>
```js /App.js
export default function App() {
  return (
    <div style={{ padding: '20px' }}>
      <h2>Simple Flow</h2>
      <svg width="400" height="200" viewBox="0 0 400 200">
        <defs>
          <linearGradient id="flowGradient" x1="0%" y1="0%" x2="100%" y2="0%">
            <stop offset="0%" stopColor="#3b82f6" />
            <stop offset="100%" stopColor="#8b5cf6" />
          </linearGradient>
        </defs>
        
        <path
          d="M 50 100 Q 200 50, 350 100"
          stroke="url(#flowGradient)"
          strokeWidth="4"
          fill="none"
          strokeDasharray="1000"
          strokeDashoffset="1000"
        >
          <animate
            attributeName="stroke-dashoffset"
            from="1000"
            to="0"
            dur="2s"
            repeatCount="indefinite"
          />
        </path>
      </svg>
    </div>
  );
}
```

```css /styles.css hidden
body {
  margin: 0;
  min-height: 100vh;
  background-color: #1a1a1a;
  background-image: radial-gradient(circle, rgba(255, 255, 255, 0.15) 1px, transparent 1px);
  background-size: 20px 20px;
}

#root {
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
}
```
</Sandpack>
</div>

---

## Key Concepts

### Stroke Dash Animation

The `stroke-dasharray` and `stroke-dashoffset` properties are the foundation of path animations. By animating the offset, we can create the illusion of a path being drawn.

### Timing and Easing

Smooth animations require careful attention to timing. CSS transitions and SVG `<animate>` elements both offer easing functions for natural motion.

---

## Next Steps

Try experimenting with different path shapes, gradients, and timing functions to create unique flow animations!
